name: Build and publish release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to publish"
        required: true
        default: "v2.0"

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.4'

      - name: Build app
        run: |
          cd "$GITHUB_WORKSPACE"
          xcodebuild -project CreateIT.xcodeproj -scheme CreateIT -configuration Release clean build

      - name: Create DMG
        run: |
          cd "$GITHUB_WORKSPACE"
          bash scripts/create_dmg.sh

      - name: Capture release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail

          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag_name }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi

          VERSION="${TAG#v}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          # DMG name matches what create_dmg.sh produces (CreateIT-vX.YbZ.dmg)
          echo "dmg_name=CreateIT-${TAG}.dmg" >> "$GITHUB_OUTPUT"
          echo "release_name=CreateIT ${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: ${{ steps.meta.outputs.release_name }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          files: dist/${{ steps.meta.outputs.dmg_name }}
